[package]
name = "distrun"
version = "0.1.0"
edition = "2024"
license = "GPL-3.0-or-later"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde-saphyr = "0.0.27"
thiserror = "2"

[dev-dependencies]
expect-test = "1"
